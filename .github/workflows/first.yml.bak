name: first

on: push

jobs:
    ubuntu:
        name: ubuntu Job
        runs-on: ubuntu-latest
        steps:
        - run: date
    windows:
        name: windows Job
        runs-on: windows-latest
        steps:
        - run: date
     macos:
        name: macos Job
        runs-on: macos-latest
        steps:
        - run: date
    depends:
        needs: [ubuntu,windows,macos]
        runs-on: windows-latest
           steps:
        - run: date